import { getApplicantData } from '../../api/AdminDashboard/dashboard';
import { useQuery } from 'react-query';
import { Applicants } from '../../types/dashboard';

// export const useGetSchedule = (): Schedule[] => {
//   const { data = [] } = useQuery(queryKeys.schedule, getSchedule);
//   return data;
// };

// export const useGetApplicants = (): Applicants => {};
